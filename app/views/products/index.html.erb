<h1>All Products</h1>
<ul>
  <% @products.each_with_index do |product, index| %>
    <p>This is the <%= index + 1 %> product</p>
    <p>Name: <%= product.name %>
    </p>
    <p>Price: <%= product.price %>
    </p>
    <% end %>
</ul>